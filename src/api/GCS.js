// import axios from "axios";


export async function getPlayableVideoUrl(accessToken, videoUrl, requestId) {

  return videoUrl;

  // if (!videoUrl || videoUrl === "" || !videoUrl.includes(requestId)) {
  //   return videoUrl;
  // }

  // console.log(accessToken, videoUrl, requestId);
  
  // const config = {
  //   headers: { Authorization: `Bearer ${accessToken}` }
  // };

  // const response = await axios.get(videoUrl, config);
  // console.log(response);
  // const blob = await response.blob();
  // const objectUrl = URL.createObjectURL(blob);
  // console.log(objectUrl);
  // return objectUrl;

}